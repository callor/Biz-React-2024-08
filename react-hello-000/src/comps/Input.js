/**
 * 사용자 정의형 컴포넌트
 * React 에서는 모듈을 분리하여 파일로 관리할 수 있다
 * 파일로 분리된 모듈을 컴포넌트라고 한다.
 * 사용자 정의형 컴포넌트는 첫글자를 반드시 대문자로 사용한다
 * 기본 tag 컴포넌트는 모두 소문자이다
 * 기본 tag 컴포넌트와 이름 충돌을 방지하기 위하여
 * 사용자 정의형 컴포넌트는 첫글자를 대문자로 사용한다
 *
 */
const Input = () => <input />;
export default Input;
